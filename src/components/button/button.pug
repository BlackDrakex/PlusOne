mixin button(options={})
  if options.type === "settings"
    button(class="button button_type_settings" type="button")
      include ./settings.svg
  else
    button(class="button" type="button")= options.text